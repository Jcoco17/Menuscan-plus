export default async (req, res) => {
  if (req.method === 'POST') {
    const { item, table } = req.body;
    console.log(`Nuevo pedido: ${item} para ${table}`);
    
    // Aquí podrías conectar a Firebase/FaunaDB
    return res.status(200).json({ 
      message: `Pedido de ${item} recibido!` 
    });
  }
  res.status(405).end(); // Método no permitido
};